import React from 'react';
import ReactDOM from 'react-dom/client';
import { CallPage } from './App/CallPage';

const root = document.getElementById('root');

ReactDOM.createRoot(root).render(<CallPage />);
